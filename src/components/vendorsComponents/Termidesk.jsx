import React from 'react';
import Logo from '../../assets/vendors/termidesk.svg';

export default function Termidesk() {
    return (
        <img src={Logo} alt="Termidesk (ООО «Ювейон» / UVEON)" className="w-full h-auto max-w-[300px] object-contain svg-icon"/>
    );
}