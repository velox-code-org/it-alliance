import React from 'react';
import Logo from '../../assets/vendors/red-soft.svg';

export default function RedSoft() {
    return (
        <img src={Logo} alt="РЕД СОФТ (АО «РЕД СОФТ»)" className="w-full h-auto max-w-[300px] object-contain svg-icon"/>
    );
}